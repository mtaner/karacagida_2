<div style="margin:50px;">
	<div class="product">
		<h4><%= t("products.#{@product.fetch("product_name")}") %></h4>
		<img src="<%= asset_url("#{@product.fetch("image_path")}")%>" width="400" height="400" alt="<%= @product.fetch("product_name") %>" />
	</div>
</div>
